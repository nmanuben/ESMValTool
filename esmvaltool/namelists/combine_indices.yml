###############################################################################
# combine_indices.yml
#
# Description
# Namelist for combining indices from multiple models and computing area averages
#
# Authors
# A Hunter (BSC)
# N Manubens (BSC)
# Project
# KNMI Magic
#
# 
# 
# 
#
# This namelist is part of the ESMValTool
###############################################################################
---

models:
  - {model: bcc-csm1-1,  project: CMIP5,  mip: Amon,  exp: rcp85,  ensemble: r1i1p1,  start_year: 2020,  end_year: 2060}


preprocessors:
  preproc:
    regrid:
      target_grid: bcc-csm1-1
      scheme: linear
    mask_fillvalues:
      threshold_fraction: 0.95

diagnostics:
  anomaly_agreement:
    description: Combine indices and/or compute area weighted averages.
    variables:
      pr:
        preprocessor: preproc
        mip: Amon
        field: T2M
    scripts:
      main:
        script: combine_indices.R #(in diag_scripts)
        
        #Region considered
        lat_max: 70
        lat_min: 27
        lon_max: 50
        lon_min: 5

        #Start and end periods for the historical and projection periods
        startdate: "2000-01-01"
        enddate: "2002-12-01"
        
        ## Weights for combining indices - if not NULL, must be the same length as the number of indices
        weights: 1, 1, 2, 0, 1
        
        ### Indices to be combined
        indices: cdd, rx5day, tx90p, tx10p, wx


